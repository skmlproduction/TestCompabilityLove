# AGENT_STATUS — TestCompabilityLove (Love Test, dev.lovetest.app)

> Живой статус для центрального агента-оркестратора и пользователя.
> Обновляй ПОСЛЕ каждого осмысленного изменения. Новые записи журнала — сверху.

## Снимок
- **Стадия:** Legal **200×3 ✅**; store validate ✅; unit ✅; canary **3/3 ✅** + spot QA hub/consent/paywall (emp_ready_p1, 2026-07-22 ~12:52).
- **Dirty tree:** ~248 локальных изменений (P1 UI/tests/docs) **не запушены** — нужен commit по запросу.
- **Next user:** Closed IAP — `docs/store/CLOSED_IAP_SMOKE.md`. Опционально: «закоммить P1» / Internal AAB upload.
- Production: после Closed IAP.

## Журнал
- 2026-07-22 ~12:53 — **Full check:** legal/repo/Pages OK; validate_store_upload OK; unit OK; Capture `emp_ready_p1` canary Consent+Love+Calc **3/3**, spot PNG **3/3**. Фикс leftover URL в LegalDocumentsTest → skmlproduction. Dirty ~248 files unpushed.
- 2026-07-22 ~12:48 — **`check_legal_urls` OK** privacy/terms/data-collection **200**. Terms починен: добавлен `terms_of_use.html` + export (`95d3ed1`).
- 2026-07-22 ~12:45 — terms 404: `terms_of_use.html` был untracked; commit+push `95d3ed1` (export пишет `terms.html`).
- 2026-07-22 ~12:40 — User: SSH as **skmlproduction**, repo created, `git push -u origin main` ✅. URL обновлён на skmlproduction.github.io. Ждём Pages deploy.
- 2026-07-20 ~06:54 — Повторные «продолжай»: legal/repo всё ещё 404. Статус → **IDLE_BLOCKED_USER**. Не гонял эмулятор/сборку.
- 2026-07-20 ~06:53 — **Continue (noop):** legal/SSH без изменений (404 + Permission denied); AVD = чужой NeonKT. Agent-scope recheck исчерпан — нужен user GitHub SSH + repo + push.
- 2026-07-20 ~06:52 — **Continue:** legal всё ещё 404; NeonKT занял AVD (чужой). `testDebugUnitTest` + `:core:domain:test` ✅. Добавлен `docs/store/CLOSED_IAP_SMOKE.md` (фаза 4). **Idle на user legal push.**
- 2026-07-20 ~06:51 — **Critical suite 11/11 ✅:** A11y 9, Love/Wheel/Missing/Victory/Share + Consent; DebugStart **20/20** + Calc/Protocol/Zodiac на snapshot `emp_ready_p1`. Логи `build/continue_critical_full_rerun.log` / `continue_critical_finish.log`. Legal 404 — agent idle на user.
- 2026-07-20 ~06:40 — **Continue:** Critical **4/4 ✅** повтор на живом LoveTester_Capture; `export_privacy_for_hosting` ✅; snapshot `emp_ready_p1` refresh. Legal всё ещё 404 — **agent-scope исчерпан**.
- 2026-07-20 ~06:39 — **P1 QA 12/12 FRESH API34** + snapshot `emp_ready_p1` сохранён. Critical 4/4 ранее ✅. Agent-scope recheck закрыт; блокер: legal push (user).
- 2026-07-20 ~06:39 — **P1 QA 12/12 FRESH API34:** 3+9 кадров на LoveTester_Capture cold boot mem=896 (`pair_input`/`consent`/`love_low` @06:34; remaining 9 @06:37–06:39). `default_boot` snapshot broken; script `scripts/_run_p1_api34_recapture.py`. HyperLock soft force-stop в `adb_screenshot_preview`.
- 2026-07-20 ~06:04 — **Critical 4/4 ✅** LoveTester_Capture (Consent 2/2, Calculator/Protocol/Zodiac 1/1). Лог `build/continue_critical_rerun.log`.
- 2026-07-20 ~05:31 — Capture install OK → Consent test Process crashed; device lost. QA audit 12/12 OK on disk.
- 2026-07-20 ~04:15 — NeonKT QA 12/12 + Consent 2/2; flows fail API26.
- 2026-07-20 ~03:40 — P1 assembleDebug ✅.
- 2026-07-19 ~21:12 — **Continue:** hitchhike мелькнул device→offline; adb install failed «device not found». Swap всё ещё ~9.7 GB. **Без reboot Mac агент-скоуп исчерпан.**
- 2026-07-19 ~21:10 — **Continue:** hitchhike иногда online; free peak ~488; swap 9 GB; pollers kill. Нужен **reboot**.
- 2026-07-19 ~19:00 — **Full recheck start:** Phase0 Xiaomi QA **34/34** RU; Phase1 review; Phase2 UI fixes; Gradle install blocked. Legal 404.
- 2026-07-19 ~14:22 — **USB install Xiaomi:** `installDebug` на `PF99SSNFFQQ4XCF6` ✅. MIUI не трогал.
- 2026-07-19 ~14:00 — **SSH unblock:** создан `~/.ssh/id_ed25519` (не было pubkey), `ssh-add` OK. Repo GitHub всё ещё 404. Connected skip (host ~73 MB). Следующее: user — GitHub → New SSH key + New repo → `git push`.
- 2026-07-19 ~13:58 — **Idle на user:** `validate_store_upload` ✅; legal/repo всё ещё 404; host ~60 MB → connected skip. Добавлен `scripts/check_github_repo.sh`. Agent-scope без create-repo/SSH исчерпан. Следующее: **пользователь** создать repo + push.
- 2026-07-19 ~13:56 — **Legal root-cause:** GitHub repo URL **404** (репо не создан или недоступен) + Pages 404; `ssh-add -l` empty; `gh` not logged in. `export_privacy_for_hosting` refreshed `build/legal-host/`. Batched: adb-install fallback + host RAM gate (&lt;200 MB skip). Connected не гонял (host ~55 MB). Critical 42/42 ранее ✅.
- 2026-07-19 ~13:55 — **Hitchhike blocked:** после `installDebug` на lite35 часто нет `dev.lovetest.app` (только `.test` + HyperLock) → Process crashed 0 tests. `testDebugUnitTest` ✅. Soft force-stop только HyperLock overlay. Critical 42/42 ранее ✅. Legal 404 — user `git push`.
- 2026-07-19 ~13:52 — **Share canary + hitchhike:** root cause HyperLock overlay (`com.hyperlockscreen.app`) steal focus → DESTROY test Activity; также packages иногда пропадали. Soft force-stop только этого overlay на hitchhike serial. Share exporter log on fail + off-window measure. MainActivity share / ComponentActivity — Process crashed («0 tests») при host free низком. `testDebugUnitTest` — см. ниже. Critical 42/42 ранее ✅. Legal 404. Чужие qemu/host apps не гасил.
- 2026-07-19 ~13:45 — **Critical fail-rerun aborted (LMK, no foreign kills):** hitchhike `emulator-5558`/lite35: 4/5 classes Process crashed («Starting 0 tests»), device briefly offline на DebugStart. Host free ~67–96 MB. Остановлен только наш thrash-batch. Фиксы: Share→`ComponentActivity`; `EnsureAppKoinRule` soft; `LoveInstrumentedCleanup` без stopKoin; DEBUG calc animation 25ms/frame; batched abort on offline + no kill-all/force-stop. Critical 42/42 ранее ✅. Production блокер: legal 404 (user `git push`).
- 2026-07-19 ~13:32 — **STOP kill foreign:** пользователь запретил гасить чужие приложения. Убраны `am kill-all` / force-stop из `_run_critical_batched.py`. Exclusive qemu-kill скрипты не запускать.
- 2026-07-19 — **AVD lite + RAM root-cause:** Capture `hardware-qemu.ini` bump до 2560MB (убивал boot). Config → 1280; **LoveTester_Lite** API35/google_apis/1024MB создан; `stable.py` предпочитает Lite. `compileDebugAndroidTestKotlin` ✅. Boot Lite/Capture всё ещё die при host free ниже ~400 MB. SSH agent socket есть, ключей нет. Блокер: RAM или user `git push`.
- 2026-07-18 — **Full connected hardening + RAM block:** full suite на MBN (115) → ~35 fails (KoinAppAlreadyStarted / scroll / ClosedScope) + crash. Фиксы: `@Before stopKoin` на input/hub/splash; `performScrollTo` на result/paywall; `EnsureAppKoinRule` stop+start; cleanup = session в starting, stop+restart appKoin в finished (конфиг critical 42/42). Re-verify AVD: free ~70 MB → stable.py **SIGKILL 137**. `testDebugUnitTest` ✅. `GO_NO_GO` обновлён. Нужно: RAM для AVD **или** user legal push.
- 2026-07-18 — **Critical connected 42/42 ✅** на hitchhike `emulator-5560` (MBN_Gate34). Фиксы: Application `runCatching` + eager inject (crash после `stopKoin`); `LoveInstrumentedCleanup` — не гасит Koin в `starting`, в `finished` stop+restart appModule; `EnsureAppKoinRule` перед MainActivity share-тестом; Victory `performScrollTo`. Exclusive LoveTester под contention нестабилен. Осталось: user `git push` → Pages 200 + Closed IAP smoke.
- 2026-07-18 — **Connected still blocked (hitchhike):** `_run_connected_hitchhike.py` дождался `emulator-5560` device, стартовал gradle → за 9s ADB closed / No connected devices. Exclusive LoveTester по-прежнему kill mid-boot. Код hardening на месте; suite не green. Дальше только idle AVD или user legal push.
- 2026-07-18 — **Connected still blocked:** после kill RegSnap free RAM ~1.7 GB, LoveTester стартует, затем чужой агент шлёт shutdown (emu log) и поднимает 2× RegSnap. `_run_connected_stable.sh` (narrow watchdog) готов. Agent-scope плана без AVD/legal исчерпан. Нужен idle AVD или user `git push` → Pages 200.
- 2026-07-18 — **Connected re-run blocked:** exclusive LoveTester не выживает (RegSnap respawn + ~58 MB free). Hitchhike `emulator-5558` → ADB EOF. `testDebugUnitTest` + `verifyLoveTestBeforeStore` ✅. Скрипты `_run_connected_critical.sh` / `_run_connected_suite.sh` готовы. Suite на AVD не закрыт. Блокеры: свободный AVD **или** user legal push.
- 2026-07-18 — **Connected suite hardening:** полный `connectedDebugAndroidTest` на AVD → 29–45 fails (каскад Koin + ComposeNotIdle + `victory_outcome_maybe="?"`). Фиксы: `LoveInstrumentedCleanup` (rule *перед* composeRule), `DebugUiPreview.clear()`, HubLoading finite pulse + determinate progress, Victory `onAllNodesWithText`, Consent `performScrollTo`, suite script: wm 1080×1920 / anim scale 0 / clear app locales. `compileDebugAndroidTestKotlin` + `testDebugUnitTest` ✅. Re-run suite: AVD boot_fail (чужие агенты убивают qemu). Legal всё ещё 404. Следующее: свободный AVD → `scripts/_run_connected_suite.sh` или user `git push` legal.
- 2026-07-18 — **Store EN 34/34 DONE (AVD):** полный recapture на exclusive `LoveTester_Capture` @1080×1920 (port 5554); архив `qa/emulator/en` **34/34**. Helper `scripts/_run_en_catalog_exclusive.sh` (гасит чужие qemu, `</dev/null` в catalog). `_capture_en_once` тоже с `</dev/null` (иначе обрыв после 1 экрана). `verifyLoveTestBeforeStore` ✅; `packStoreUpload` + listing 7×EN обновлены. Блокер: legal 404.
- 2026-07-18 — **Store RU 34/34 DONE (AVD):** добиты hub/error_network/ad_interstitial; root cause wrong-route — blind ANR Wait-tap кликал Hub→Love input. `adb_screenshot_preview` dismiss ANR только при диалоге. EN key: hub/love/share/paywall. Архив `qa/emulator/ru` **34/34**. Exclusive LoveTester (другие AVD гасились на время съёмки). Блокер: legal 404.
- 2026-07-18 — **AVD follow-up:** Victory/Letters E2E ✅; route **20/20** + share **2/2**. LoveTester_Capture @1080×1920: 25 clean RU frames → store + `qa/emulator/ru`. Concurrent HyperLock/MatchByNames AVD → System UI ANR / wrong-route frames — store restored for polluted set; `adb_screenshot_preview` теперь dismiss ANR + reject cool-grey overlay. Полный 34/34 store recapture отложен (RAM/contention). Блокер: legal 404.
- 2026-07-18 — **Quality pass follow-up DONE:** Pair/Wheel/Protocol/MissingSession E2E; CriticalFlowsA11y **9/9** (hub Koin + `stopKoin`); `navHostTestModule` + HubViewModel/ads mocks; docs `FULL_APP_AUDIT`/`GO_NO_GO`/`CAPTURE_BUILD_STAMP`/`WCAG` обновлены. AVD: снят overlay `com.hyperlockscreen.app` (мешал тестам). Xiaomi USB сейчас offline — spot QA не снимал. Системные настройки телефона не менялись. Блокер: legal 404.
- 2026-07-18 — **Phases 7–8 DONE:** ViewModel invalid-input unit tests; Wheel PNG share (`captureWheelShareCard`); hub cards merged semantics; DebugStartRoute **20** routes/previews; Calculator/Zodiac/Love E2E + `navHostTestModule`/`stopKoin`; share exporter instrumented ✅; local scripts без `--no-daemon`; `verifyLoveTestBeforeStore` ✅. Xiaomi spot hub/share/wheel. Системные настройки не менялись. Блокер: legal 404.
- 2026-07-18 — **Quality pass close:** `NameInputHint` RU/EN; `LoveTonalButton` + Outlined `enabled`; chips Role.Button; Wheel `backEnabled`; share icons + scrim dismiss без magic 220dp; interstitial `Idle/Loading/Ready/Failed` + consume on Failed; CTA **перед** decorative preview; Xiaomi catalog **34/34** (retry calculator_result после Play overlay). Системные настройки не менялись. Блокер: legal 404.
- 2026-07-18 — **Full quality pass DONE:** `NameInputValidator` + ViewModel guard на love/calc/pair/victory/letters/protocol; chips/ad-close ≥48dp; home/share semantics; result ellipsis; Zodiac low cards; high preview before CTA; restore Restored/NotFound/Unavailable; interstitial retry loop; unit tests ✅; `installDebug` Xiaomi; key QA **5/5**. Системные настройки не менялись. Блокер: legal 404 (push у пользователя). Готов к следующему промпту.
- 2026-07-18 — **Планы закрыты / idle на legal:** redesign+polish done; recheck — Pages всё ещё 404; `~/.ssh` без pubkey, GitHub SSH deny. `build/legal-host/` готов. Дальше только пользователь: создать SSH key или HTTPS PAT → commit/push → Pages → `./scripts/check_legal_urls.sh`.
- 2026-07-18 — **Phase 3 QA gate close:** полный Xiaomi RU catalog **34/34** → `qa/xiaomi/ru` (store `docs/screenshots/ru` не трогали, остаётся 1080×1920); `verifyLoveTestBeforeStore` ✅; `AUDIT_REPORT`/`GO_NO_GO_V1` обновлены. Системные настройки не менялись. Планы redesign+polish закрыты. Блокер: legal push (пользователь).
- 2026-07-18 — **Tasteful polish pass 3 (plan close):** Onboarding pages — `fillMaxWidth` (не `fillMaxSize`) + `LaunchedEffect scrollTo(0)` — hero снова в первом viewport (welcome/tests/protocol/disclaimer). Xiaomi QA ×4 OK. Системные настройки не менялись. В скоупе плана больше нечего; блокер релиза — legal push у пользователя.
- 2026-07-18 — **Tasteful polish pass 2:** Onboarding protocol — hero 216dp + softWrap body; steps Column (Вердикт без переноса); hint «+6 в хабе»; bottom pad 40dp. Tests grid — `onboarding_ob_love_cap` «Два имени». Xiaomi QA: onboarding_tests / onboarding_protocol. Системные настройки не менялись. План polish по визуальным клипам закрыт; дальше — legal push (пользователь).
- 2026-07-18 — **Tasteful polish (план):** Love input hero — single softWrap body, minH 128dp, heart 40dp; Onboarding mini-cards — 2-line captions (короче RU/EN) + minH 152dp; Wheel footer + `navigationBarsPadding`. `installDebug` Xiaomi + QA: love_test_input / onboarding_welcome / wheel_spin. Системные настройки не менялись. Следующее: legal push (пользователь) или дальше по оркестратору.
- 2026-07-18 — **Store pack refresh:** `pack_store_upload` + validate ✅ (listing 7×RU/EN @1080×1920, AAB ~8.4 MB); legal-host export OK; `check_legal_urls` всё ещё 404. Диагностика: `git@github.com` → **Permission denied (publickey)** — агент не может push. `GO_NO_GO_V1` + `GITHUB_FIRST_PUSH` обновлены. Телефон не трогал. Следующее: **пользователь** `git push -u origin main` + Pages Actions → `./scripts/check_legal_urls.sh`.
- 2026-07-18 — **Xiaomi key QA ✅:** после ручного unlock — 5 экранов в `qa/xiaomi/ru` (splash/hub/love_input/protocol_input/paywall); два кадра сначала ушли в Play Store → `adb_screenshot_preview` + focus check + force-stop vending; recapture OK. `testDebugUnitTest` ✅. Системные настройки не менялись. Следующее: **git push** → GitHub Pages legal 200; Closed IAP smoke.
- 2026-07-18 — **Phone QA blocked:** Xiaomi `isKeyguardShowing=true` / Dozing / NotificationShade — black frames rejected; blank 5 QA PNG removed. `adb_screenshot_preview` + near-black reject (Pillow sample); `LOVETEST_SCREENSHOT_OUT_DIR` для QA. Legal export `build/legal-host/` готов; Pages всё ещё 404 (нужен `git push` + Actions). Следующее: **разблокировать телефон вручную** → capture 5: splash/hub/love_input/protocol_input/paywall; или push legal.
- 2026-07-18 — **Post-redesign hardening:** global `~/.gradle/gradle.properties` + `-XX:-TieredCompilation` → `lintDebug` ✅ + `verifyLoveTestBeforeStore` ✅; listing pack v2 + fixed ANR `love_test_input` RU; `pack_store_upload` OK. Xiaomi: `adb install` OK, spot screencap blocked (NotificationShade/lock, black frames) — **без** смены system settings. Следующее: разблокировать телефон вручную → QA, или git push legal Pages.
- 2026-07-18 — **Stylish redesign Phases 0–3 DONE:** light editorial romance; `DESIGN_SYSTEM.md` + 34 SVG `docs/design/v2/`; Compose token migration; love-input hero fix; Xiaomi QA без system settings; Store RU+EN **34/34 @1080×1920**; `AUDIT_REPORT`/`GO_NO_GO_V1` обновлены. Блокер: legal 404.
- 2026-07-18 — **Stylish redesign Phases 0–2:** palette ink/paper/rose/teal; `DESIGN_SYSTEM.md` v2; 34 SVG `docs/design/v2/`; Compose tokens + heroes (Letters→rose, no Material purple); love input hero fix (`fillMaxWidth` vs `matchParentSize`); Xiaomi RU QA + full catalog **34/34**. Phase 3: EN @ LoveTester_Capture 1080×1920 (AVD height был 2400 — восстановлен 1920) + gates.
- 2026-07-18 — **Typography/top-bar pass 1 (Xiaomi):** `LoveFeatureTopBar` — maxLines=2 + padding; `LoveScreenBackButton` softWrap=false; `AppTitle` 34→20sp; onboarding skip minWidth; RU `love_test_result_low_tag` → «Ещё есть надежда». `installDebug` + QA 6 PNG на PF99SSNFFQQ4XCF6. Следующее: hero body на love input, onboarding mini-card captions.
- 2026-07-14 — **Production completion audit:** финальный аудит + `FULL_APP_AUDIT.md`/`AUDIT_REPORT.md` (P0=0); Hub grid subtitle maxLines=2; R8/minify ON; RU+EN 34/34 @1080×1920 (эмулятор); `bundleRelease` + `pack_store_upload` + `verifyLoveTestBeforeStore` ✅. Блокер: legal 404 (git push + Pages).
- 2026-07-14 — **Post-plan EN capture:** AVD `LoveTester_Capture`; capture scripts hardened; EN 34/34 @1080×1920.
- 2026-07-14 — **Post-plan gate (lint):** JBR C1 crash на Apple Silicon → `-XX:-TieredCompilation` в `gradle.properties`; `verifyLoveTestBeforeStore` ✅.
- 2026-07-14 — **Фаза 10** (финал): EN `capture_screenshot_catalog.sh` — 14 свежих + 20 restore из git; listing repack; `GO_NO_GO_V1.md` + `AUDIT_REPORT.md` обновлены. **План 0–10 закрыт.**
- 2026-07-14 — **Фаза 9** (QA gate): `capture_screenshot_catalog.sh ru` 34/34; 13 кадров восстановлены из QA после bad frames; sync `qa/xiaomi/ru`; gates OK кроме legal/lint.
- 2026-07-14 — **Фаза 8** (Share + listing): `ShareResultPreview`/`LoveShareCard`/`ShareActionsPanel`/`WheelSharePreview`/`WheelShareCard` → typography tokens + edge padding; percent через `percentForRing`; muted variant сохранён; listing 7×RU из QA Xiaomi; `share_result_card` recapture; `pack_store_upload` OK.
- 2026-07-14 — **Фаза 7** (Premium + Settings): `PremiumPaywallScreen`/`PremiumThankYouScreen`/`SettingsScreen` → `loveEdgeToEdgeScreenPadding`, typography tokens; paywall/thank-you heroes `ResultHeroShape`; Consent debug banner → `CardCaption`. Monetization honesty (support dev benefits при ADS off) без изменений. `compileDebugKotlin` + `installDebug` + QA 3 PNG.
- 2026-07-14 — **Фаза 6** (Zodiac + Wheel): `ZodiacPickScreen`/`ZodiacResultScreen`/`WheelSpinScreen`/`WheelResultScreen` → `LoveFeatureTopBar`, edge-to-edge; zodiac pick hero без изменений; result heroes `ResultHeroShape`; `FieldLabel`/`HubHeroChip`/`HeroBody`/`CardCaption` tokens; wheel spin back блокируется при spin. `compileDebugKotlin` + `installDebug` + QA 4 PNG.
- 2026-07-14 — **Фаза 5** (Protocol): `ProtocolInputScreen`/`ProtocolResultScreen` → `LoveFeatureTopBar`, edge-to-edge; input hero unchanged (`HubHeroShape`); result hero `ResultHeroShape`; `FieldLabel`/`HubHeroChip`/`HeroBody`/`CardCaption` tokens; empty state edge padding. `compileDebugKotlin` + `installDebug` + QA 4 PNG.
- 2026-07-14 — **Фаза 4** (Calculator/Pair/Letters/Victory): 4 input + 4 result → `LoveFeatureTopBar`, edge-to-edge; input heroes `HubHeroShape`; result heroes `ResultHeroShape`; `FieldLabel`/disclaimer tokens; Calculator info → pink container. `compileDebugKotlin` + `installDebug` + QA 8 PNG.
- 2026-07-14 — **Фаза 3** (Love test flow): `LoveFeatureTopBar` (back + center title); Input — edge-to-edge, hero `HubHeroShape` 67dp, `FieldLabel`/tip tokens; Calculating/Result — edge-to-edge + typography tokens; Result cards → `ScreenHeadline`/`HeroBody`. `compileDebugKotlin` + `installDebug` + QA 4 экрана.
- 2026-07-14 — **Фаза 2** (Hub): `HubScreen` — `AppTitle`, `HubPremiumTitle`/`HubPremiumSubtitle`; welcome hero `heightIn(min=93dp)` + chip/heart sizing; featured/protocol cards + grid `heightIn`, grid icons 28dp, title maxLines=2. `compileDebugKotlin` + `installDebug` + QA `hub_main`.
- 2026-07-14 — **Фаза 1** (Splash + Onboarding): Splash — убран дубль `splash_headline_*`, chips `weight(1f)` + `SplashFeatureChipHeight`; Onboarding welcome/tests — `ScreenHeadline`/`HeroBody`/`CardTitle`/`CardCaption`; tests grid `heightIn(min=72dp)` + title maxLines=2; `SplashScreenComposeTest` → hero line. `compileDebugKotlin` + `installDebug` Xiaomi OK; QA recapture splash/onboarding.
- 2026-07-14 — **Фаза 0** (Design foundation): `LoveTypographyTokens` — AppTitle, ScreenHeadline, HeroTitleOnGradient; `LoveEdgeToEdgeTopBar` + skip fix; Splash/Onboarding/Consent typography + Consent rx=48; DESIGN_SYSTEM.md. `compileDebugKotlin` OK.
- 2026-07-14 — Xiaomi Visual QA: 34 PNG `docs/screenshots/qa/xiaomi/ru/`; install debug OK.
- 2026-07-14 — Release build: `isMinifyEnabled`/`isShrinkResources` → **false** (временно, для локального теста).
- 2026-06-22 — Промпт 10/10 (Visual QA + Store): эмулятор LoveTester_Capture 1080×1920; `capture_visual_qa_ru.sh` 17/17 PNG (часть с System UI ANR); `recapture_store_screenshots.sh` RU 34/34, EN 8/34 (эмулятор отвалился); ANR-corrupt listing PNG восстановлены из git; ~22 RU PNG post-polish сохранены. `verifyLoveTestJvm`/inventory/PNG gate OK; `verifyLoveTest`/`lintDebug` — daemon OOM; `check_legal_urls.sh` → 404. Матрица `AUDIT_REPORT.md` 34/34 code+SVG ✅.
- 2026-06-04 — Промпт 9/10 (A11y): SettingsRow mergeDescendants+CD; `CriticalFlowsA11yComposeTest` (input/back/hub/wheel/onboarding/result/settings); WCAG checklist обновлён; hub/wheel/settings compose tests расширены. `assembleDebug` — см. ниже.
- 2026-06-04 — Промпт 8/10 (Share): `LoveShareCard` → bitmap (`ShareCardImageExporter`) + `ACTION_SEND image/png`; FileProvider; TG/WA target package; muted card `high=false`; `ShareResultPreviewComposeTest` расширен. `assembleDebug` — см. ниже.
- 2026-06-04 — Промпт 7/10 (Premium + Settings vs screen24–26): ads-off paywall benefits «Поддержать разработку» (не «Все тесты»/«Без рекламы»); thank you/hub/settings copy; consent banner только DEBUG; dynamic price test; `BILLING_SETUP.md` SKU rename note. `assembleDebug` — см. ниже.
- 2026-06-04 — Промпт 6/10 (Zodiac + Wheel vs screen20–23): `LoveZodiacAccentPink`/`LoveZodiacShareBorder`; pick hero `HubHeroShape` 67dp + star decor + `HeroTitle`/`HeroBody`; slot2/sign2 pink tokens; result ring 200dp + cosmic badges/share; wheel segment colors in `Color.kt`; filled gold `WheelPointer`; spin/result badge tokens; `WheelNotBetFooter` kept (screen22). `assembleDebug` — см. ниже.
- 2026-06-22 — Промпт 5/10 (Protocol vs screen30–34): input hero `HubHeroShape` rx46 + `HeroTitle`/`HeroBody` + min 73dp; signals grid Material icons (check/warn/dot); verdict bands 0/1/2 colors; low warning+tip; `LoveFeatureResultActions` (pink share high / teal low); share preview `percentForRing`; `ProtocolFlowComposeTest` + result low test. `assembleDebug` — очередь gradlew lock.
- 2026-06-22 — Промпт 4/10 (Calculator/Pair/Letters/Victory vs screen12–19): `LoveTypographyTokens` на 4 input heroes; `FeatureLowWarningCard`/`FeatureLowTipCard` на low% results; Pair metrics bar+value layout (screen15); Letters tiles 44dp + FCE4EC highlight (screen19); Victory YES/MAYBE outcome chips (screen17); `FeatureResultLogic` + unit tests; compose tests low warning. `assembleDebug` — очередь gradlew lock.
- 2026-06-22 — Промпт 3/10 (Love test flow vs screen8–11, screen27): `LoveTestResultScreen` → `LoveFeatureResultActions` (+ optional `disclaimerRes`); share preview только high (screen11 без preview); mini preview percent через `percentForRing(80dp)`; input hero `HeroTitle`/`HeroBody`; inline tip disclaimer (screen8) + compose test; `LoveTestFlowComposeTest` (input→calculating→result). `assembleDebug` — очередь gradlew lock.
- 2026-06-22 — Промпт 2/10 (Hub vs screen6/33): hub tokens (hero 93dp, grid 59dp/8dp gap, featured 67dp, protocol 80dp); `LoveTypographyTokens` hub/card styles; welcome hero typography; featured GO pill; protocol NOVOE+№8 chip+teal GO; grid badges; bottom nav 36dp. `compileDebugKotlin` — очередь gradlew lock.
- 2026-06-22 — Промпт 1/10 (Design foundation): матрица header patterns в DESIGN_SYSTEM.md; edge-to-edge на Onboarding×4; `LoveTypographyTokens` (section/hero/percent); `ResultHeroShape` 48dp для screen10–11; `HeroShape` 54dp на остальных hero cards; Splash/Onboarding/Consent typography. `assembleDebug` OK; `core:ui:lintDebug` OK; `app:lintDebug` — daemon OOM (retry).
- 2026-06-22 — **Повторный полный аудит** (по запросу пользователя): пересмотр всех 34 экранов vs 56 SVG; функции/monetization/store; обновлён `FULL_APP_AUDIT.md` + 10 промптов v2. PNG дата 2026-05-24; legal 404; IAP honesty 🔴.
- 2026-06-21 — Промпт 9/10 (A11y + inputs): `loveInputFieldSemantics` / `loveInputLabelForAccessibility` на всех text input screens; `LoveScreenBackButton` (mergeDescendants) на back; Hub bottom nav merge + selected state; Zodiac sign cells semantics; wheel CD уже на месте; `docs/a11y/WCAG_CRITICAL_FLOWS_CHECKLIST.md`. `assembleDebug` + tests OK; `connectedDebugAndroidTest` — META-INF/LICENSE merge (packaging fix) / device.
- 2026-06-21 — Промпт 8/10 (Premium, Settings, monetization copy): ads-off paywall copy; dynamic price; Consent debug banner; Settings polish. `assembleDebug` + tests OK.
- 2026-06-21 — Промпт 7/10 (Zodiac + Wheel): Zodiac lane tokens в `Color.kt`; ZodiacPick/Result polish; Wheel segment labels, `WheelNotBetFooter`, result hero. `assembleDebug` + tests OK.
- 2026-06-21 — Промпт 6/10 (Calculator, Pair, Letters, Victory): `LoveFeatureResultActions` на 4 result; `FeatureHeroTallMinHeight` 148dp; Pair metrics fallback; Letters combined stream; Victory yes/maybe. `assembleDebug` + tests OK.
- 2026-06-21 — Промпт 5/10 (Protocol flow): ProtocolInput — teal hero + Test №8 badge, steps row, fields 48dp, tip container, teal CTA, disclaimer; ProtocolResult — percent ring, summary grid (check/warn/neutral), verdict bands, low warning+tip, muted hero, share preview; fallback при `protocolSignals == null` + empty session UI; strings RU/EN screen30–34. `assembleDebug` OK.
- 2026-06-21 — Промпт 4/10 (Love test flow): tokens `LoveTestInputFieldHeight`/`ResultRing`/`CalculatingRing`/`ShareCardHeight`; input fields 48dp + inline tip disclaimer (screen8); calculating ring 134dp/stroke 8dp, ghost 32sp, step dots 12dp; result ring 200dp scaled typography; share card 387dp + 108sp; `ShareResultPreview` bottom sheet panel. `assembleDebug` + unit test OK.
- 2026-06-21 — Промпт 3/10 (Hub + protocol strip): HubScreen → screen6/screen33 — welcome hero (120dp, rx 46), featured love card + 2×3 grid (68dp cells), teal `HubFeaturedProtocolCard` (96dp, NOVOE, GO pill); Settings только в bottom nav (убран top-bar IconButton); `hub_loading`/`error_network` — debug-only (`BuildConfig.DEBUG` + `DebugUiPreview`); strings RU hub cards. `assembleDebug` OK (~13s up-to-date); `adb_screenshot_preview.sh hub_main ru` — adb нет в среде агента.
- 2026-06-21 — Промпт 2/10 (Splash + Onboarding vs SVG): hero splash rx 54 + shadow; onboarding hero heights по screen2–4/35/36; tests grid 2×3 (68dp cells, 12dp gap); welcome mini-cards + accent bar; protocol teal theme, steps row, hub GO card, after-banner; disclaimer protocol strip (screen36); strings RU/EN. `assembleDebug` OK; connected tests — очередь gradlew.
- 2026-06-21 — Промпт 1/10 (design foundation): `LoveLayout` (24dp / 54dp / 56dp CTA), `LoveFeatureHero`, `loveEdgeToEdgeScreenPadding()` на Splash, Onboarding×4, Consent, Premium, ThankYou; hero на love/pair/protocol input; Premium CTA 56dp; DESIGN_SYSTEM.md → Compose tokens. Fix: `LoveHeroCard` import, `matchParentSize` BoxScope. `assembleDebug` OK; `core:ui:lintDebug` OK; `app:lintDebug` — Metaspace OOM (retry).
- 2026-06-04 — Полный аудит UI/функций vs SVG: `docs/product/FULL_APP_AUDIT.md` (34 экрана, P0–P2 backlog); 10 промптов доработки — пользователю.
- 2026-06-04 — Промпт 7/7: `GO_NO_GO_V1.md` — Internal **GO**, Production **NO-GO**; runbook Internal + чеклист Closed; формы Data safety / IARC (ads off, wheel ≠ gambling).
- 2026-06-04 — Промпт 6/7: terms_of_use.html + Pages export; URL в PLAY_CONSOLE_COPY и `gradle.properties`; Premium → privacy/terms в браузере; `check_legal_urls.sh`, `GITHUB_FIRST_PUSH.md` (команды push).
- 2026-06-04 — Промпт 5/7: prod keystore fix — `keystore.properties` указывал debug `android`; в CREDENTIALS `keyPassword` ≠ `storePassword` → Gradle «bad padding». Синхронизировано с `CREDENTIALS.local.txt` (key=store); `bundleRelease` OK; `pack_store_upload` + `validate_store_upload` OK; SHA-256 upload cert см. снимок.
- 2026-06-04 — Промпт 4/7: коммит UI-полировки (NoActionBar, insets, hero heightIn, muted low %, экраны hub/splash/onboarding/settings + results + pair/letters/victory input); скрипты пересъёмки PNG; `.gitignore` — секреты не в staging.
- 2026-06-04 — Промпт 3/7: пересъёмка Store PNG **не выполнена** (нет adb/device); `docs/screenshots/` **не изменены** (34+34 от ~05-24, до UI-фиксов). PNG gate + `validate_store_upload` OK на **старых** кадрах. Добавлены `recapture_store_screenshots.sh`, `RECAPTURE_AFTER_UI_FIX.md`.
- 2026-06-04 — Промпт 2/7: adb нет в среде агента — runtime QA не прогонялся; статический аудит фиксов OK; `scripts/capture_visual_qa_ru.sh` + `*_result_low` в DebugUiPreview для пересъёмки muted hero.
- 2026-06-04 — Промпт 1/7: `assembleDebug` OK (~3m39s, daemon, build-lock); compile-фиксов не потребовалось (`LoveScoreCalculator.isHighScore` уже в companion). `lintDebug` app + core:ui OK; замечаний lint по изменённым UI/Kotlin файлам нет. APK: `app/build/outputs/apk/debug/app-debug.apk` (~25 MB).
- 2026-06-04 — Промпт 10: `PRODUCTION_ROLLOUT.md`, `POST_RELEASE_PLAN.md`, `V2_BACKLOG.md`; go/no-go NO-GO prod до signing/closed.
- 2026-06-04 — Промпт 8 (продолжение): AAB собран; upload-пакет полный, validate OK. Prod keystore: mismatch пароля → временно debug key.
- 2026-06-03 — Промпт 7: ASO-тексты RU/EN в PLAY_CONSOLE_COPY; 7 listing PNG честны (1080×1920, disclaimer на result/protocol/wheel); PNG gate OK; adb нет — пересъёмка не делалась.
- 2026-06-03 — Промпт 6: Data safety + IARC сверены с кодом (v1 без ads/аналитики); `PLAY_FORMS_FILLED.md`, обновлены DATA_SAFETY_FORM, IARC_QUESTIONNAIRE, PLAY_CONSOLE_COPY.
- 2026-06-03 — Промпт 5: аудит Billing 8.3 (`remove_ads`, acknowledge, restore); ads off (`ADS_ENABLED=false`, AD_ID manifest не мержится); DATA_SAFETY уточнён (v1 без AD ID). `verifyLoveTest` — в очереди gradlew lock.
- 2026-06-03 — Промпт 4: проверен `.gitignore` + `validate_git_staging`; commit `8638dcc` (AGENT_STATUS, orchestration.mdc, gradlew lock, CI-safe gradle.properties, `.kotlin/` в ignore). **Push failed** (no GitHub credentials). Инструкция push — пользователю.
- 2026-06-03 (вечер) — AGENT_STATUS, privacy URL, prod keystore, AAB не собран.
- 2026-06-03 — GO_LIVE_PLAN, монетизация v1, git init + 3 commits, remote настроен.
