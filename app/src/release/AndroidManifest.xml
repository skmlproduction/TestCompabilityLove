<?xml version="1.0" encoding="utf-8"?>
<!--
  Overlay release build type (подмешивается только в release).
  Реклама в v1 выключена (lovetest.ads.enabled=false,
  BuildConfig.ADS_ENABLED=false, AdsBootstrap возвращается сразу), но
  play-services-ads находится в classpath и подмешивает в манифест
  рекламные разрешения и MobileAdsInitProvider (автозапуск AdMob SDK
  при старте процесса). Убираем их, чтобы бинарник соответствовал
  Data safety декларации. При включении рекламы в release — удалить этот файл.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_TOPICS"
        tools:node="remove" />

    <application>
        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            tools:node="remove" />
        <provider
            android:name="com.google.android.gms.ads.MobileAdsInitProvider"
            tools:node="remove" />
    </application>
</manifest>
