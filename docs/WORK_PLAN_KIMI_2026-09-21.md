# План работы: Love Tester → Production

Дата: 2026-09-21. Автор: Kimi. Перекрывает/уточняет `docs/NEXT_DEVELOPMENT_PLAN_2026-09-09.md` (LT-01…LT-05) с учётом свежего аудита.

## Исходная точка (проверено фактом)

- AAB `1.0.0 (1)` пересобран 07.09, подписан, SHA-256 `f063ca80…48073b`; legal pages живые (HTTP 200×3, проверено 19.09).
- 70 unit app + 9 core/domain тестов PASS, lint 0 errors / 24 intentional warnings, target 36, 16KB alignment PASS.
- ⚠️ **Git dirty: 69 изменённых файлов + 7 untracked (включая новые тесты и план) — работа за 06–09.09 не закоммичена.** Последний коммит `fddb4f0`.
- ⚠️ Скриншоты `docs/screenshots/{en,ru}` от 18.07 — **stale**: после них были accessibility/CTA-изменения (07.09). Сняты 1080×1920, дизайн v2 рассчитан на 1080×2400.
- ⚠️ `build/store-upload/` пакет ≠ текущий release AAB (LT-01 подтверждён).
- Монетизация: `lovetest.ads.enabled=false`, SKU `remove_ads` — paywall подаёт это как «tip jar / поддержка разработчиков».
- ADB пуст: ни эмулятора, ни Xiaomi.

## Найденные дефекты дизайна (по скриншотам 18.07, перепроверить на свежей сборке)

| ID | Экран | Дефект | Приоритет |
|----|-------|--------|-----------|
| D1 | Onboarding welcome (EN) | Сердце-декор **перекрывает заголовок** «Check your♥ love match» | P1 |
| D2 | Love test input | CTA «Calculate love» обрезан нижним краем (disabled state у gesture bar) | P1 |
| D3 | Hub | Карточки внизу («By letters» и др.) обрезаются за bottom nav, текст рвётся mid-word | P2 |
| D4 | Fantasy wheel | Указатель-треугольник грубый (чёрный+золото, вне палитры); подсказка внизу обрезана gesture bar | P2 |
| D5 | Premium paywall | Hero-градиент уходит в оранжево-янтарный — вне rose-палитры v2 | P2 |
| D6 | Общее | Скриншоты 1080×1920, не 9:19.5 как v2-макеты; на длинных экранах контент упирается в gesture area | P2 |

Дизайн-система в целом сильная: светлая editorial romance (paper/ink/rose), свои lane-палитры для зодиака (violet night) и протокола (teal), Inter, консистентные радиусы. Полный редизайн НЕ нужен — точечные фиксы + свежая съёмка.

## Фазы работы

### Фаза 0 — Гигиена репозитория (P0, 1 сессия)
1. Разобрать git dirty: закоммитить осмысленную работу 06–09.09 (тесты, план, verify_release_artifact.py) отдельными атомарными коммитами. Спросить владельца перед коммитом.
2. Удалить `hs_err_pid*.log` / `replay_pid*.log` из корня (мусор JVM-крешей), добавить в `.gitignore`.

### Фаза 1 — Релизный пакет LT-01 (P0, 1 сессия)
1. `verifyLoveTestRelease` полный прогон (подтвердить 79 unit / lint / R8).
2. Пересоздать согласованный upload-пакет: свежий AAB + mapping.txt + метаданные + манифест одной сборки; старый `build/store-upload/` пометить ARCHIVED, не удалять.
3. Обновить `docs/store/INTERNAL_UPLOAD_NOW.md` на проверенный комплект (SHA, versionCode, cert fingerprint).

### Фаза 2 — Дизайн-фиксы D1–D6 (P1, 1–2 сессии)
1. Поднять эмулятор (API 35/36), установить debug, воспроизвести D1–D6 на живой сборке — часть могла быть исправлена adaptive-pass 07.09.
2. Пофиксить подтверждённые: минимальные диффы в `core/ui` и экранах onboarding/input/hub/wheel/paywall.
3. Прогнать unit + lint + androidTest compile.

### Фаза 3 — Сценарный и визуальный QA (P1, нужно устройство)
1. Smoke-матрица: ввод → результат → share → повтор × 8 тестов; пустой ввод, пробелы, длинные имена, RU/EN; поворот, перезапуск, offline.
2. Accessibility: TalkBack, font scale 1.3/2.0, маленький экран 360×640, landscape.
3. Пересъёмка полного store-сета 34×2 (RU/EN) на свежей сборке с locale/marker-гигиеной (правила из LockDraw playbook).

### Фаза 4 — Честность монетизации LT-03 (P0 до paid track, решение владельца)
- Конфликт: SKU называется `remove_ads`, реклама выключена, paywall продаёт «поддержку разработчиков».
- Варианты: (а) переименовать SKU в `premium_tip` в Play Console и оставить честный tip-jar; (б) включить рекламу для free и продавать remove_ads по-настоящему. **Решение — за владельцем, код под оба варианта готов.**
- После решения: license-tester smoke покупки/restore/pending/отмены на Internal track.

### Фаза 5 — Store выпуск (owner + агент)
1. Агент: release notes, свежие скриншоты → fastlane/пакет, проверка Data safety против включённых SDK, GO/NO-GO memo.
2. Владелец: upload AAB → Internal → smoke → Closed (12 тестеров, 14 дней для новых аккаунтов) → Production.
3. После Production GO: вернуться к V2-бэклогу (AdMob, deeplinks).

## Правила работы (наследую из проекта)
- Gradle с daemon, без `--no-daemon`; одна тяжёлая сборка за раз (Mac 16 GB, соседние проекты едят RAM).
- Не трогать чужие qemu/AVD; не стартовать V2 до Production GO.
- После каждого осмысленного изменения обновлять `AGENT_STATUS.md` (новые записи сверху).
- Не коммитить и не менять внешние настройки без подтверждения владельца.

## Ближайший шаг
Фаза 0 + Фаза 1: коммит dirty-работы → полный verify → согласованный upload-пакет. Дальше Фаза 2 (дизайн).
